# Next.js frontend reads this URL to call Flask directly.
NEXT_PUBLIC_FLASK_API_URL=http://127.0.0.1:5000

# Flask backend reads this path to load the trained model.
# If unset, Flask defaults to ../artifacts/best_random_forest_tuned.joblib
MODEL_PATH=
